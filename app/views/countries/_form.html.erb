<%= form_for(@country, html: {multipart: true}) do |f| %>
  <% if @country.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@country.errors.count, "error") %> prohibited this country from being saved:</h2>

      <ul>
      <% @country.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :english_name %><br>
    <%= f.text_field :english_name, class: "english_name" %>
  </div>
  <div class="field">
    <%= f.label :flag %><br>
    <%= f.file_field :flag %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
